
  # Modern Authentication Onboarding Flow

  This is a code bundle for Modern Authentication Onboarding Flow. The original project is available at https://www.figma.com/design/9uByiqlvFUy5OsByKXnUyK/Modern-Authentication-Onboarding-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  